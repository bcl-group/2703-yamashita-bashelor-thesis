# 卒業研究 現在地整理（2026-08-25時点）

本資料は「卒業研究プロジェクト 引き継ぎ仕様書」に基づき、`2703-yamashita-bashelor-thesis` フォルダ（ソースコード、README、PROGRESS.md、Git履歴、関連論文、学習ノート）を調査した結果をまとめたものである。仕様書の記述だけでなく、実際のコード・コミット履歴を確認したうえで作成している。

---

## 調査した内容

- フォルダ構成（`.git`、関連論文、個人用、勉強したこと（必見）などのサブフォルダ）
- Git履歴（全コミットのログ・日付・差分）
- README.md / PROGRESS.md（現状記述）
- `勉強したこと（必見）/数値解析/HH.py`（Hodgkin-Huxleyモデル実装）
- `勉強したこと（必見）/数値解析/南雲解析.py`・`.md`（FitzHugh-Nagumo型モデルの数値解析）
- `勉強したこと（必見）/深層学習ゼミ/3章.py`・`詳解ディープラーニング.md`（教科書の写経ノート）
- `勉強したこと（必見）/猿でもわかるシリーズ/猿でもわかるニューロン発火...md`（HHモデル解説資料）
- 関連論文フォルダ（Hodgkin & Huxley 1952 原著、棟近先輩の卒論・スライド、南雲論文）

---

## 【研究の目的】

マルチコンパートメント神経モデル（空間形状を考慮した神経細胞モデル）を対象に、その時間発展をRNNで近似するサロゲートモデルを構築し、元の数値シミュレーションより高速に神経活動を予測できるようにすること。空間計算量の削減とGPUによる高速化の両立を狙う。

## 【現在の研究仮説】

RNNのhidden state（内部状態）が、現在の入力電流と過去から蓄積された情報を反映しながら膜電位の時間発展を逐次的に予測できる、という仮説。ただし、hidden stateが生物学的なゲート変数（m, n, h）そのものを表しているとは断定していない（README・仕様書とも明記）。

## 【使用している神経モデル】

- **HHモデル（単一コンパートメント）**：実装済み。`HH.py` はEuler法（`dt=0.05`, `time=100`）で膜電位・ゲート変数(m, n, h)を計算。パラメータは `Cm=1.0, gNa=120.0, gK=36.0, gl=0.3, ENa=50.0, EK=-77.0, El=-54.387` で、元論文値と一致。
  - 補足：`HH.py` のα/β式は `v - self.rest`（静止電位からのシフト）を用いる実装になっており、READMEに記載の連立微分方程式（シフトなしのV表記）とは変数系の対応が一目では分かりにくい。値を変更する話ではないが、両者の対応関係を一度整理しておくと卒論執筆時に説明しやすい。
- **南雲（FitzHugh-Nagumo型）モデル**：`南雲解析.py` で実装済み。パラメータ `a=0.7, b=0.8, c=10`、周期パルス電流入力（`I0=0.1601` など）に対する膜電位応答をEuler法で計算。HHモデルの簡略版としての基礎検証と位置づけられる。
- **マルチコンパートメントモデル**：**未実装**。コード・ドキュメントともに見当たらない。READMEには「Multi Compartment実装 〜2026年8/10」と目標が書かれているが、本日（8/25）時点で着手されていない。

## 【RNNモデル】

READMEには「PyTorchを使った簡単なRNNを実装し、sin波の学習に成功した（25ステップ分の過去波形を1ステップずつずらして入力）」と記載され、画像（image-5.png）も残っている。

**重要な発見**：このRNN実装コード自体は、リポジトリ内のどこにも存在しない。コミット履歴を全件検索したが、RNNやモデル定義に該当するPythonファイルは一度も追加されていない（コミット `902234d "RNN実装"` はPROGRESS.mdのテキスト更新のみで、コードの追加はなし）。Google Colabなど別環境で実行された可能性が高い。

`3章.py`（深層学習ゼミフォルダ）は「詳解ディープラーニング」の教科書写経で、単純パーセプトロンの実装途中（`np.random.nomal` のタイプミス、`def foward(self.x):` の構文エラーなど未完成）。これはRNN実装とは別物で、研究本体のコードではなく学習ノートの位置づけ。

→ **HH×RNNフェーズに進むための実装コードは、現時点でこのフォルダには一切存在しない。**

## 【入力】

想定：HHモデルへのパルス入力電流 $I(t)$。教師データ生成のためのコードは未実装。

## 【出力】

想定：膜電位 $V(t)$。学習が難しい場合は $g, m, n, h, \alpha, \beta$ などの内部パラメータも学習対象に加える案がREADMEに記載されているが、実装はまだ先の話。

## 【学習方法】

未実装。PROGRESS.mdの「近々やること」に「HH教師データの作成」「HH教師データの学習」が挙げられている段階。

## 【評価方法】

未定。README・PROGRESSに具体的な評価指標の記述はない。仕様書には評価候補（波形、RMSE、MAE、相関、スパイクタイミング、発火回数、計算時間、推論速度、空間的な電位分布）が挙げられているが、採用は指導教員の方針次第、との位置づけのまま止まっている。

## 【現在得られている結果】

- HHモデル単体の数値シミュレーション：完了。発火波形とゲート変数(m, n, h)の挙動を確認し、「猿でもわかるニューロン発火」資料としてまとめ済み。
- 南雲(FitzHugh-Nagumo)モデルの数値解析：完了。周期パルス入力に対する応答を `I0=0.16` / `0.5` の2条件で比較済み。
- RNN基礎実装：sin波の時系列予測に成功（画像のみ確認、実装コードは未保存）。
- HH×RNN、マルチコンパートメント、マルチコンパートメント×RNN：いずれも未着手。

## 【未実装】

1. RNN実装コードそのもの（リポジトリへの保存）
2. HH教師データ生成コード（パルス電流→HH出力のペア生成、train/test分割）
3. HH×RNNの学習コード（loss記録、学習曲線の可視化含む）
4. マルチコンパートメントモデルの実装
5. マルチコンパートメント×RNNのサロゲート学習
6. 評価パイプライン（元モデル vs RNNサロゲートの比較一式）

## 【問題点】

1. **マイルストーン遅延**：README記載の「Multi Compartment実装 〜2026年8/10」は期限超過（本日8/25）で未着手。
2. **コミットの停滞**：最終コミットは2026-07-31で、以降約3週間半、研究コードの更新がない。PROGRESS.mdには大学院入試（学科専門試験8/19-21）が「困っていること」として記載されており、これが影響している可能性がある。
3. **RNN実装コードの所在不明**：README記載の成果（sin波学習）を再現・引き継ぐための元コードが見つからない。まずこれを探す/復元する必要がある。
4. HH.pyの変数系（シフトあり）とREADMEの数式（シフトなし）の対応関係が資料上明示されていない。

## 【次にやるべき実験】

1. RNN実装コード（sin波学習に使ったもの）の所在確認・リポジトリへの保存
2. HHモデルへのパルス電流入力による教師データ生成コードの実装
3. train/test分割方針の決定
4. HH×RNNの学習実装（forward propagation → loss → BPTT → gradient → parameter update の一連を、ブラックボックス化せず理解しながら実装）
5. マルチコンパートメントモデルの選定・実装（指導教員から紹介された論文の確認が前提、本人判断が必要な項目）
6. 評価指標の確定（指導教員と相談）とHH×RNNでの比較実験

## 【卒論に必要な追加検証】

- 元モデル（HH、将来的にはマルチコンパートメント）とRNNサロゲートの精度比較（波形・誤差指標）
- 計算時間比較（CPU/GPU、バッチサイズ、warm-upなど計測条件を必ず記録）
- マルチコンパートメントにおける空間的な電位伝播の再現性評価
- 再現可能なグラフ生成コードとパラメータ記録の整備

---

### 補足：フォルダ内の非研究コンテンツ

`個人用（あんまり見ないで）` フォルダには大学院入試・教員採用試験・語学ゼミなど、卒業研究とは無関係な個人学習資料が含まれている。今回の調査ではこれらの内容には立ち入っていない。
